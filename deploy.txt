1. Buat email di outlook.com.
2. Sign up Github.
3. Buat repo private beri nama "buku tahfiz".
4. Download Web App ZIP di: https://drive.google.com/drive/folders/13DKYr1_SQAgrwC7lduk2eHteRiIqW0ym?usp=sharing
5. Ekstrak dan taruh di desktop.
6. Buka Github Desktop → Push bukutahfiz-v239-webapp ke Github.

7. Buka VS Code → File → Open Folder → bukutahfiz-v239-webapp.

8. Vs Code: Terminal:

npx wrangler login

npx wrangler whoami

npx wrangler d1 create auth01
npx wrangler d1 create auth02
npx wrangler d1 create auth03
npx wrangler d1 create auth04
npx wrangler d1 create auth05
npx wrangler d1 create auth06
npx wrangler d1 create auth07
npx wrangler d1 create auth08
npx wrangler d1 create auth09
npx wrangler d1 create auth10

npx wrangler d1 execute auth01 --remote --file .\bukutahfiz-d1-auth01-main-console.sql

2..10 | ForEach-Object {
  $db = "auth{0:D2}" -f $_
  Write-Host "Executing archive schema to $db..."
  npx wrangler d1 execute $db --remote --file .\bukutahfiz-d1-auth02-auth10-archive-console.sql
}

9. Buka di browser: https://dash.cloudflare.com/.
10. Masuk menggunakan akun Github.
11. Account home (dash.cloudflare.com): Add → Pages → Import an existing Git repository → Get Started → Repo: "bukutahfiz" → Begin setup → Project name: https://bukutahfiz.pages.dev → Save and Deploy.
12. dash.cloudflare.com: Workers and Pages → "bukutahfiz" → Settings → Build configuration → "Tanda pensil" → Framework preset → Nuxt.js → Save.
13. dash.cloudflare.com: Workers and Pages → Settings → Variables and Secrets:

WHATSAPP_DEVELOPER → 08996830011
USERS_PAGE_USERNAME → admin
USERS_PAGE_PASSWORD → @de88RTD (bebas)
PAID_LIFETIME_CODE → @de88RTD (bebas)
PAID_YEARLY_CODE → @de88RTD (bebas)
PAID_CODE_PEPPER → @de88RTD (bebas)
RESET_CODE_PEPPER → @de88RTD (bebas)
ARCHIVE_WORKER_URL → https://archive-manager.{bukutahfiz}.pages.dev
ARCHIVE_WORKER_TOKEN → @de88RTD (harus sama dengan ADMIN_TOKEN Workers)

14. Account home (dash.cloudflare.com): Add → Workers → Start with hello world! → Worker name: https://archive-manager.bukutahfiz.workers.dev → Deploy.
15. dash.cloudflare.com: Workers and Pages → "archive-manager" → </> Edit code → Copy dan paste: https://github.com/raportahfiz/archivemanager/blob/main/workers.js → Deploy.
16. dash.cloudflare.com: Workers and Pages → "archive-manager" → Settings → Variables and Secrets:

ADMIN_TOKEN → @de88RTD (bebas)

17. dash.cloudflare.com: Workers and Pages → "archive-manager" → Settings → Trigger Events:
Cron expression → 8 14 * * * (artinya pukul 14:08 + 7 jam WIB)

18. dash.cloudflare.com: Workers and Pages → "archive-manager" → Bindings → Add binding → + Binding:

DB01 → auth01
DB02 → auth02
DB03 → auth03
DB04 → auth04
DB05 → auth05
DB06 → auth06
DB07 → auth07
DB08 → auth08
DB09 → auth09
DB10 → auth10

19. dash.cloudflare.com: Workers and Pages → "bukutahfiz" → titik tiga (...) → Retry deployment → Success.
20. Tes aplikasi klik: bukutahfiz.pages.dev

Catatan: setiap kali ganti token/password di "Variables and Secrets" harus "Retry deployment".

